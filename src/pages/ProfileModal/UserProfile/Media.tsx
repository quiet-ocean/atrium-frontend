import { Box } from '@mui/material'
import { AtText } from '../../../components'

import { Container as PContainer } from './styled'

export const Media = () => {
  return (
    <PContainer>
      <AtText className="subtitle">media</AtText>
    </PContainer>
  )
}
